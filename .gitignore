# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.env
.env.local
.env.*.local

node_modules
dist
dist-ssr
*.local

# Estado local del CLI de Supabase (se regenera solo)
supabase/.temp/

# Dumps de schema de referencia (no versionados; la fuente de verdad es supabase/migrations/)
schema_actual.sql
schema_propio.sql

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea

# Tooling local de Claude Code (skills, scripts de esas skills) — no es
# código del proyecto, es config de la herramienta con la que se trabaja acá.
# Estaba comiteado por error y hacía que GitHub contara Python como ~35% del
# repo (los scripts de las skills), cuando Vallis no usa Python para nada.
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
